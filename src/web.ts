import {repl} from './repl.js';

// TODO - maybe be more like ersatz? code in one pane, output in another,
// then a bottom line for one-off evaluations?
const out = document.getElementById('out');
const prompt = document.getElementById('prompt');
